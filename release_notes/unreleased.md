**Unreleased**

* Corrected the US East region mappings so Ohio uses `us-east-2` and N. Virginia uses `us-east-1`. Existing assets using either selection will begin sending STS requests to the selected region after upgrading.
